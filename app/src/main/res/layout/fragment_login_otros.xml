<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.arnauzapata.myapplication.LoginOtros">
    <Button
        android:id="@+id/buttonCambiarContrasenya"
        android:text="Cambiar Contraseña"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/buttonBorrarUsuario"
        android:text="Borrar Usuario"
        android:layout_below="@+id/buttonCambiarContrasenya"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/buttonAtras"
        android:text="Atras"
        android:layout_below="@+id/buttonBorrarUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
